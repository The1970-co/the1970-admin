"use client";
import MobileBottomNav from "@/components/mobile/MobileBottomNav";
import { apiJson } from "@/lib/api";
import { API_BASE } from "@/lib/api-base";
import { ArrowLeft,PackageOpen,Search,X } from "lucide-react";
import Link from "next/link";
import { useEffect,useMemo,useState } from "react";
async function api<T=any>(p:string){return apiJson<T>(p,{redirectOnUnauthorized:false} as any)}
function asset(u?:string|null){if(!u)return "";return /^https?:\/\//.test(u)?u:`${API_BASE}${u.startsWith("/")?"":"/"}${u}`}
export default function Page(){const [rows,setRows]=useState<any[]>([]),[q,setQ]=useState(""),[detail,setDetail]=useState<any>(null),[error,setError]=useState("");useEffect(()=>{api<any[]>("/production/accessories").then(setRows).catch(e=>setError(e.message))},[]);const list=useMemo(()=>{const k=q.toLowerCase().trim();return k?rows.filter(x=>[x.code,x.name,x.typeName].some(v=>String(v||"").toLowerCase().includes(k))):rows},[rows,q]);return <main className="min-h-[100dvh] bg-neutral-100 pb-[calc(112px+env(safe-area-inset-bottom))]"><div className="mx-auto max-w-md"><header className="sticky top-0 z-20 border-b bg-white px-4 pb-4 pt-[calc(16px+env(safe-area-inset-top))]"><div className="flex items-center gap-3"><Link href="/mobile/production" className="grid h-10 w-10 place-items-center rounded-full bg-neutral-100"><ArrowLeft className="h-5 w-5"/></Link><div><div className="text-[10px] font-black uppercase tracking-[.18em] text-neutral-400">Sản xuất</div><h1 className="text-xl font-black">Nguyên phụ liệu</h1></div></div><div className="relative mt-4"><Search className="absolute left-3 top-3.5 h-4 w-4 text-neutral-400"/><input className="w-full rounded-2xl border py-3 pl-10 pr-3 text-base" value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm mã, tên, loại NPL..."/></div></header><div className="space-y-3 p-4">{error&&<div className="rounded-2xl bg-rose-50 p-3 text-sm text-rose-700">{error}</div>}{list.map(x=>{const image=asset(x.imageUrl);return <button key={x.id} onClick={()=>setDetail(x)} className="flex w-full gap-3 rounded-[26px] bg-white p-4 text-left shadow-sm"><div className="h-20 w-20 shrink-0 overflow-hidden rounded-2xl bg-neutral-100">{image?<img src={image} className="h-full w-full object-cover" alt=""/>:<div className="grid h-full place-items-center"><PackageOpen className="h-7 w-7 text-neutral-300"/></div>}</div><div className="min-w-0"><div className="text-xs font-black text-neutral-400">{x.code} · {x.typeName}</div><div className="mt-1 font-black">{x.name}</div><div className="mt-2 text-sm">Tồn: <b>{Number(x.stockQty||0).toLocaleString("vi-VN")}</b> {x.unit}</div></div></button>})}</div></div>{detail&&<div className="fixed inset-0 z-50 bg-black/45 p-3"><div className="mx-auto mt-6 max-w-md rounded-[30px] bg-white p-4"><div className="flex justify-between"><div><div className="text-xs font-black text-neutral-400">{detail.code}</div><div className="text-lg font-black">{detail.name}</div></div><button onClick={()=>setDetail(null)} className="grid h-10 w-10 place-items-center rounded-full border"><X className="h-4 w-4"/></button></div><div className="mt-4 grid grid-cols-2 gap-3"><I l="Loại" v={detail.typeName}/><I l="Tồn" v={`${Number(detail.stockQty||0).toLocaleString("vi-VN")} ${detail.unit}`}/><I l="Đơn giá" v={detail.unitPrice?`${Number(detail.unitPrice).toLocaleString("vi-VN")}đ`:"—"}/><I l="NCC" v={detail.supplierId||"—"}/></div>{detail.note&&<div className="mt-3 rounded-2xl bg-neutral-50 p-3 text-sm">{detail.note}</div>}</div></div>}<MobileBottomNav/></main>}
function I({l,v}:{l:string;v:any}){return <div className="rounded-2xl bg-neutral-50 p-3"><div className="text-[10px] font-bold uppercase text-neutral-400">{l}</div><div className="mt-1 text-sm font-black">{v}</div></div>}
